from django.apps import AppConfig


class ProfilesConfig(AppConfig):
    """
    Configuration class for the 'profiles' app.

    This class defines configuration settings for the 'profiles' app, including its name.

    Attributes:
        name (str): The name of the 'profiles' app.
    """
    name = 'profiles'
