from django.apps import AppConfig


class LettingsConfig(AppConfig):
    """
    Configuration class for the Lettings application.

    Attributes:
        name (str): The name of the application, typically set as 'lettings'.
            This is used by Django to identify the application.
    """
    name = 'lettings'
